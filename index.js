"use strict";

const medumba = require("./lib/medumba");

const ghomala = require("./lib/ghomala"); // call for participants
const nufi = require("./lib/nufi"); // call for participants

module.exports = {
    byv: medumba,
    bbj: ghomala,
    fmp: nufi,
};
